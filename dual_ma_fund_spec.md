# 双均线基金回测 · 参数规格书（SPEC）

> 用途：把"双均线策略接 12 只基金做可交互回测"这件事需要的**全部参数**列清楚，
> 逐项标注状态，让你一眼看出**还缺哪些关键参数**。
> 配套代码：`dual_ma_lib.py`（回测引擎）、`双均线策略回测.ipynb`（交互面板）、`fund_data/*.csv`（12 只基金数据）。

## 状态图例

| 标记 | 含义 |
|---|---|
| ✅ 已定 | 代码已实现，有默认/已取数 |
| ⚠️ 待确认 | 需要你拍板，否则暂用占位默认（可能与真实情况不符） |
| ❌ 缺失 | 未实现，需补代码才能用 |
| 🔧 已存疑 | 数据/口径有偏差，结果只作近似 |

---

## 决策已落地（用户 2026-07-10 确认，已全部实现）

原 6 个开放问题（Q1~Q6）**全部关闭**，代码已落地：

1. **忽略表中 3 列数字** → 不再需要费率表，基金费率按行业默认。
2. **基金费率按行业默认**：ETF 佣金万 0.5（无最低、无印花税）、100 份整手；
   场外 申购费 0.1% + 赎回费分档（<7 天 1.5% / 7 天~1 年 0.5% / ≥1 年 0）、份额小数；
   股票 佣金万 2.5 + 印花税千 1 + 100 股（杰瑞股份）。
3. **数据从 2023-01-01 起；成立日晚的从成立日取** → 已重新抓取 12 只基金
   （2023-01-01 ~ 2026-07-10）；`022430` 从 2024-10-31、`021958` 从 2024-11-04 起。
4. **加止盈止损 + TP/SL 网格分布** → 引擎新增 `stop_loss`/`take_profit`
   （bar *i* 检测、bar *i+1* 开盘执行，防前视）；新增 `tp_sl_grid` + `plot_tp_sl_heatmap`。
5. **先用 SMA** → 均线类型保持 SMA（未实现 EMA）。
6. **保持单基金对比** → 不做组合/轮动；面板与扫描均为单标的。

**代码变更清单**：
- `dual_ma_lib.py`：增加 `cost_model`(etf/otc/stock)、`fractional`、赎回费分档、TP/SL、`tp_sl_grid`/`plot_tp_sl_heatmap`。
- `dual_ma_lib.py`：修复 `load_price_csv`——基金日期 `20230103` 为整数，被 `pd.to_datetime` 误当纳秒时间戳解析成 1970 年（对抗审查发现）；改为统一转字符串去横线后按 `%Y%m%d` 解析。
- `build_notebook.py`：接入 12 基金 + 基金版成本/TP/SL 控件 + TP/SL 网格 cell；默认回测起点 2023。
- `fetch_fund_data.py`：改为按 `ts_code` 内容匹配、取 mtime 最新文件（重新抓取后自动用新数据）。

**剩余 minor（不影响主流程）**：QDII（`513050`）净值 T+1/T+2 时滞未在引擎单列建模（暂按 T+1）；
ETF 未做红利再投资调整（轻微低估）。

---

## A. 数据层

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| A1 | 数据源 | ✅ 已定 | 本地 `fund_data/*.csv`，12 只基金 2024-01-01 ~ 2026-07-10 全到齐。来源 Tushare（本机无 westock/token）。 |
| A2 | 价格字段 | ✅ 已定 | ETF/LOF 取市场价 OHLC；场外基金 `close=复权净值(adj_nav)`，`open=high=low=close`（单日一净值）。格式与杰瑞股份 CSV 一致。 |
| A3 | 复权口径 | 🔧 已存疑 | 场外用 adj_nav（含分红再投，总收益口径）。**ETF 用的是未复权市价**，没做红利再投资调整，会轻微低估。 |
| A4 | 回测区间 | ⚠️ 待确认 | 默认全样本、面板可改。但 `022430`(2024-10-31 起)、`021958`(2024-11-04 起) 历史短。**跨基金统一窗口取哪个？** 见开放问题 Q3。 |
| A5 | QDII 净值时滞 | ❌ 缺失 | `513050 中概互联ETF` 是 QDII，净值有 T+1/T+2 时滞。引擎目前把 ETF 当 T+1 开盘价等价处理，未单独建模。 |

---

## B. 策略层

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| B1 | 短均线周期 short | ✅ 已定 | 默认 **5**，面板可调（3~60）。 |
| B2 | 长均线周期 long | ✅ 已定 | 默认 **15**，面板可调（5~120）。 |
| B3 | 均线类型 | ⚠️ 待确认 | 当前仅 **SMA**。是否要 EMA / WMA 变体？未实现。 |
| B4 | 信号规则 | ✅ 已定 | 金叉（短上穿长）= 买，死叉（短下破长）= 卖；用"穿越瞬间"事件，非持续状态。 |
| B5 | 持仓方向 | ✅ 已定 | 仅**多头**（long），无做空。 |
| B6 | 加/减仓 | ⚠️ 待确认 | 当前**满仓 / 空仓**二值切换，无部分仓位。是否需要按信号强度分批？ |

---

## C. 执行 / 成本层（⚠️ 当前是 A 股模型，基金必须换）

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| C1 | 佣金率 | ❌ 缺失（A 股模型） | 默认万 2.5 双边（A 股）。基金不适用：ETF/LOF 用**券商佣金**（无印花税），场外基金用**申购费/赎回费**。 |
| C2 | 印花税率 | ❌ 缺失（A 股模型） | 默认千 1（仅卖）。**基金无印花税**，需置 0。 |
| C3 | 最低佣金 | ⚠️ 待确认 | 默认 5 元。基金可能无最低，或按申购金额比例。 |
| C4 | 滑点 | ✅ 已定 | 默认 0，面板可调（0~1%）。 |
| C5 | 整手 / lot | ❌ 缺失（A 股模型） | 默认 100 股。基金：ETF 场内 100 份/手（或 1 份起），场外基金最低 10 元 / 100 元起购。 |
| C6 | T+1 / T+0 | ✅ 已定 | 次日开盘价成交（防前视）。ETF 场内 T+1、场外 T 日申赎 T+1 确认，口径一致。 |
| C7 | **基金费率（申购费 / 赎回费）** | ❌ **完全缺失** | 你表中每只基金后跟的 3 个数字（如 `022430: 3.00% / 20.00% / -17.00%`）**疑似费率/收益，但含义未确认** → 直接决定成本模型。见 Q1。 |

---

## D. 风控层

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| D1 | 止损 | ⚠️ 待确认 | 当前**无止损**。是否加固定止损 / 跟踪止损？ |
| D2 | 止盈 / 回撤止盈 | ⚠️ 待确认 | 当前**无止盈**。是否加？ |
| D3 | 集中度 / 最大持仓 | ✅ 已定 | 单标的，同一时刻只持 1 只基金。 |
| D4 | 停牌 / 休市处理 | ⚠️ 待确认 | 引擎用最后收盘价估值。基金净值中断罕见，但 QDII 有海外假期休市，需确认是否要容错。 |

---

## E. 组合 / 对比层

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| E1 | 单标 vs 多标组合 | ⚠️ 待确认 | 当前单基金回测 + 参数扫描。**是否要做多基金轮动/组合？** 见 Q6。 |
| E2 | 对照基准 | ✅ 已定 | 默认同基金"买入持有"（含同等费用）。 |
| E3 | 跨基金对比维度 | ✅ 已定 | 累计收益 / 夏普 / 最大回撤 / 胜率 / 交易笔数，扫描热力图已实现（目前只在杰瑞上跑过）。 |

---

## F. 展示 / 部署层

| # | 参数 | 当前状态 | 说明 / 待确认 |
|---|---|---|---|
| F1 | **SYMBOLS 接入 12 只基金** | ❌ **未接** | 笔记本 `SYMBOLS` 目前只有"杰瑞股份"。需把 `fund_data/*.csv` 加进字典才能在下拉框选基金。这一步本机已可改，等你确认费率模型后一并做。 |
| F2 | 交互面板成本控件 | ⚠️ 待确认 | 当前控件是"单边佣金 / 印花税 / 滑点"。基金应改为"申购费 / 赎回费 / 滑点"。 |
| F3 | GitHub Pages（Voilà） | ✅ 已定 | `.github/workflows/deploy.yml` 已备，选 GitHub Actions 即自动发布。 |
| F4 | 数据随仓库提交 | ⚠️ 待确认 | `fund_data/*.csv` 需和笔记本一起 push，控件才能读到基金。 |

---

## ⚠️ 卡进度的开放问题（需你确认后才能量产 12 基金对比）

- **Q1（最关键）**：你给的每只基金后 3 个数字（如 `022430: 3.00% / 20.00% / -17.00%`）分别代表什么？
  我猜可能是「申购费 / 赎回费 / 某收益」，但不确定。请明确每列含义，或给一份标准的基金费率表。
- **Q2**：ETF/LOF 与场外基金分别用哪套费用？需要具体费率；若没有，是否允许我用行业常见默认值
  （如 ETF 佣金万 0.5、场外申购 0.1%~1.5% / 赎回按持有期分档）？
- **Q3**：跨基金统一回测区间取哪个？
  (a) 各基金自有起点（区间长度不一，不可比）；
  (b) 统一从 2024-11-04 起（覆盖最晚成立的 021958，所有基金区间一致）；
  (c) 你指定一个日期。
- **Q4**：是否要加止损 / 止盈？纯双均线 = 无风控，回撤会偏大。
- **Q5**：是否要 EMA 等均线变体？还是只用 SMA？
- **Q6**：保持"单基金对比"，还是要做多基金组合 / 轮动？

---

## 最小可跑清单（确认 Q1~Q3 后即可）

1. 把 `fund_data/*.csv` 接入 `SYMBOLS`（F1）。
2. 在 `dual_ma_lib.py` 里把 A 股成本模型换成**基金成本模型**（C1/C2/C5/C7）：
   - ETF/LOF：`buy_comm=sell_comm=券商佣金`，`sell_tax=0`，`lot=100`。
   - 场外：`申购费`(买)、`赎回费`(卖，按持有期分档)，`sell_tax=0`，`lot=1`(按金额)。
3. 面板成本控件改为申购费/赎回费/滑点（F2）。
4. 统一回测区间（Q3）+ 补 QDII 时滞（A5，可选）。
5. 跑 12 基金双均线对比，出标准三件套 + 交互网页。

> ⚠️ 以上内容由 AI 基于公开信息整理生成，仅供参考，不构成任何投资建议或个股推荐。投资有风险，决策需谨慎。
